USE COACHING_INSTITUTE;
SELECT * FROM DEPT;
SET SQL_SAFE_UPDATES=0;
BEGIN;
UPDATE DEPT SET SUBJECT = "BUSINESS" WHERE SUBJECT = "ACCOUNT";
SELECT * FROM DEPT;
ROLLBACK;
SELECT * FROM DEPT;
COMMIT;
ROLLBACK;
SELECT * FROM DEPT;
