CREATE DATABASE OFFICE;
USE OFFICE;
CREATE TABLE EMPLOYEES(
ID INT PRIMARY KEY,
NAME VARCHAR(90),
MANAGER_ID  INT);
INSERT INTO EMPLOYEES (ID,NAME,MANAGER_ID)
VALUES
(1,"MADHAV",NULL),
(2,"SAM",1),
(3,"ARJUN",2),
(4,"SHIVA",3),
(5,"KESHAV",4),
(6,"TOM",5),
(7,"DAMODAR",6);

WITH RECURSIVE MY_CTE AS (
SELECT ID ,NAME,MANAGER_ID
FROM EMPLOYEES
WHERE ID=7
UNION ALL
SELECT  EMPLOYEES.ID,EMPLOYEES.NAME,EMPLOYEES.MANAGER_ID 
 FROM EMPLOYEES 
JOIN MY_CTE ON
EMPLOYEES.ID= MY_CTE.MANAGER_ID)
SELECT * FROM MY_CTE;
