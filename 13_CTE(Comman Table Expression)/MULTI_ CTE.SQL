USE RESULT;
WITH MY_CTE AS (
SELECT max(MARKS)
FROM (SELECT  MARKS
FROM B_TECH
WHERE CITY = "DELHI") AS TEMP 
),
MYCTE_2 AS(
SELECT NAME ,MARKS FROM B_TECH
WHERE MARKS>(SELECT avg(MARKS)
FROM B_TECH))
SELECT * FROM MY_CTE,MYCTE_2;
