SELECT MODE, count(CUSTOMER_ID)
FROM PAYMENT
GROUP BY MODE
HAVING count(MODE)>1
ORDER BY MODE;
SELECT MODE, count(CUSTOMER_ID)
FROM PAYMENT
GROUP BY MODE
HAVING count(MODE)<4
ORDER BY MODE;
SELECT MODE, count(CUSTOMER_ID)
FROM PAYMENT
GROUP BY MODE
HAVING max(MODE)=4
ORDER BY MODE;